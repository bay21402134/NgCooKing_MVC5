@model IEnumerable<NgCooking_Asp.net.Models.Categories>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nameToDisplay)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nameToDisplay)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.categoriesId }) |
            @Html.ActionLink("Details", "Details", new { id=item.categoriesId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.categoriesId })
        </td>
    </tr>
}

</table>
